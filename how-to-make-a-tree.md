# How to make a tree?

		   root
		 /       \
	       node     node
	       / \       / \
	    node node node node


Pointer - address in memory
Object - Class - combination of data and some functions that we can perform on the data.

- create a template class *node*
- each node has some useful information
- each node has unique identifier (int)
- each node a vector that has a list of pointers of all the children and the parent

# Task 1
**Tree sweeper**: Make a tree; write a funtion that takes in the tree root then outputs all the id of the branches.

# Task 2
**Tree hight**: calculate the tree hight.
